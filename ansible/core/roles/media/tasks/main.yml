---
- include_vars: "vars/{{ environment }}.yml"

- name: ensure required packages are installed
  apt:
    name: "{{ item }}"
    state: latest
  with_items:
    - git
    - ca-certificates
    - mysql-server
  register: result
  until: result|success
  retries: 5
  delay: 10
